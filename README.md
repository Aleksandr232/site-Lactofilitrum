# Lactofilitrum - Сайт с админкой

Этот проект представляет собой сайт компании Lactofilitrum с административной панелью на PHP и MySQL.

## Структура проекта

```
lactofilitrum/
├── index.html          # Главная страница сайта
├── login.html          # Страница авторизации
├── admin.php           # Главная страница админ-панели
├── podcasts.php        # Страница управления подкастами
├── css/
│   ├── style.css       # Стили главной страницы
│   ├── login.css       # Стили страницы авторизации
│   └── admin.css       # Стили админской панели
├── js/
│   ├── main.js         # JavaScript главной страницы
│   ├── login.js        # JavaScript страницы авторизации
│   └── admin.js        # JavaScript админской панели
├── php/
│   ├── config.php      # Конфигурация и функции
│   ├── database.sql    # SQL скрипт для создания БД
│   ├── init.php        # Автоматическая инициализация БД
│   ├── login.php       # Обработка авторизации
│   ├── logout.php      # Выход из системы
│   ├── auth_check.php  # Проверка авторизации
│   └── api/
│       ├── users.php   # API для управления пользователями
│       ├── dashboard.php # API для данных дашборда
│       ├── activity.php  # API для логов активности
│       └── content.php   # API для управления контентом
├── data/
│   └── content.json    # Хранение контента сайта
└── images/             # Папка для изображений
```

## Установка и настройка

### 🚀 Быстрый старт

1. **Установите веб-сервер** с поддержкой PHP и MySQL (XAMPP, MAMP, LAMP)
2. **Разместите файлы** проекта в корневой папке веб-сервера
3. **Настройте подключение** к базе данных в `php/config.php`:

```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'lactofilitrum_db');
define('DB_USER', 'root');
define('DB_PASS', '');
```

4. **Запустите инициализацию** базы данных: `http://localhost/php/init.php`
5. **Готово!** База данных будет создана автоматически

### 📋 Подробная настройка

#### Настройка базы данных

**База данных создается автоматически!** Вам не нужно вручную создавать базу данных или таблицы.

Однако, если вы хотите использовать собственные настройки подключения, обновите файл `php/config.php`:

```php
define('DB_HOST', 'localhost');      // Хост MySQL сервера
define('DB_NAME', 'lactofilitrum_db'); // Имя базы данных (будет создано автоматически)
define('DB_USER', 'your_username');  // Пользователь MySQL
define('DB_PASS', 'your_password');  // Пароль MySQL
```

#### Автоматическая инициализация

При первом запуске системы база данных будет инициализирована автоматически. Также вы можете запустить инициализацию вручную:

1. Откройте в браузере: `http://localhost/php/init.php`
2. Нажмите кнопку "Инициализировать базу данных"
3. Система автоматически создаст:
   - Базу данных `lactofilitrum_db`
   - Таблицы `users`, `login_logs`, `podcasts`
   - Администратора по умолчанию (`admin` / `admin123`)

#### Настройка веб-сервера

Проект использует PHP 7+, поэтому вам понадобится веб-сервер с поддержкой PHP и MySQL:

**Для Windows:**
- XAMPP (рекомендуется)

**Для macOS:**
- MAMP

**Для Linux:**
- LAMP stack

#### Размещение файлов

Разместите все файлы проекта в корневой папке веб-сервера:
- XAMPP: `C:\xampp\htdocs\lactofilitrum\`
- MAMP: `/Applications/MAMP/htdocs/lactofilitrum/`

### 4. Права доступа

Убедитесь, что папка `data` имеет права на запись для веб-сервера.

## Использование

### 🔧 Инициализация системы

**Первый запуск:**
1. Откройте: `http://localhost/php/init.php`
2. Нажмите "Инициализировать базу данных"
3. Система автоматически настроит все необходимое

**Проверка состояния:**
- Скрипт `init.php` показывает текущее состояние базы данных
- Можно запускать многократно без потери данных

### 🔐 Доступ к админке

1. Перейдите на страницу авторизации: `http://localhost/login`
2. Используйте учетные данные администратора:
   - **Логин:** `admin`
   - **Пароль:** `admin123`

**Защита админки:** Админская панель доступна только авторизованным пользователям с ролью администратора. Попытка прямого доступа перенаправит на страницу авторизации.

### Функционал админки

- **Дашборд**: Общая статистика системы
- **Подкасты**: Отдельная страница управления подкастами с экспертами

## API endpoints

- `GET/POST /php/api/users.php` - Управление пользователями
- `GET /php/api/dashboard.php` - Данные дашборда
- `GET /php/api/activity.php` - Логи активности
- `GET/POST/DELETE /php/api/podcasts.php` - API управления подкастами
- `GET /podcasts` - Страница управления подкастами

## ✨ Новые возможности

### Автоматическая инициализация базы данных
- **Автоматическое создание БД:** База данных и таблицы создаются при первом запуске
- **Интеллектуальная проверка:** Система проверяет существование всех компонентов
- **Веб-интерфейс:** Удобный интерфейс для инициализации через `php/init.php`
- **Безопасность:** Не перезаписывает существующие данные

### Улучшенная архитектура
- **Модульная структура:** Легко расширяемая архитектура
- **Автоматические миграции:** База данных обновляется автоматически
- **Отказоустойчивость:** Система продолжает работать при проблемах с БД

## Безопасность

- Все пароли хешируются с помощью `password_hash()`
- Используется подготовленные запросы для защиты от SQL-инъекций
- Валидация и очистка входных данных
- Защита от CSRF атак
- Логирование всех попыток входа
- Автоматическая инициализация без риска потери данных

## Разработка

Для запуска в режиме разработки:

1. **Установка:**
   - Запустите веб-сервер с PHP и MySQL
   - Разместите файлы в корневой папке сервера

2. **Инициализация:**
   - Откройте `http://localhost/php/init.php`
   - Нажмите "Инициализировать базу данных"

3. **Использование:**
   - Главная страница: `http://localhost/`
   - Админка: `http://localhost/login` (admin/admin123)
   - Управление подкастами: `http://localhost/podcasts`

## Автор

Проект создан для компании Lactofilitrum.

## Лицензия

Этот проект предназначен только для внутреннего использования компании Lactofilitrum.